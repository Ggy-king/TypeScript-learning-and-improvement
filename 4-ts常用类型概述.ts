let ages: number = 18

ages = '20'  //此处ts会标明错误

// ts的优点就是可以显示标记出代码出现的意外情况

age.toFixed()

{
    // 1 js已有类型
    //     原始类型： number/string/boolean/null/undefined/symbol
    //     对象类型：object(包括数组、对象、函数等)
    // 2 ts新增
    //     联合类型、自定义类型(类型别名)、接口、元组。字面量类型、枚举、void、any等
}